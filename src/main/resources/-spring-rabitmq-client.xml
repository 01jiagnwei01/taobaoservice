<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/rabbit
           http://www.springframework.org/schema/rabbit/spring-rabbit.xsd
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd">
     
       <rabbit:template id="amqpClientTemplate" 
  			connection-factory="rabbitConnectionFactory"  
		    reply-timeout="2000" 
		    message-converter="jsonMessageConverter" 
		    routing-key="app.stock.request"
		  
     > </rabbit:template>
     
     <bean id="stockClientServiceGateway" class="com.gxkj.taobaoservice.controllers.demo.stocks.gateway.RabbitStockServiceGateway">
			<property name="rabbitTemplate" ref="amqpClientTemplate" />
		    <property name="defaultReplyTo" value="#{traderJoeQueue.name}/" />
	</bean>
     
     
 
	<listener-container concurrency="5"  id="messageListener" connection-factory="rabbitConnectionFactory" message-converter="jsonMessageConverter"
		xmlns="http://www.springframework.org/schema/rabbit">
		<listener ref="clientHandler" method="handleMessage" queue-names="#{marketDataQueue.name},#{traderJoeQueue.name}" />
	</listener-container>


     <rabbit:queue id="traderJoeQueue" name="traderJoeQueue"/>
	 <rabbit:queue id="marketDataQueue" name="marketDataQueue"/>
	 
     
     <rabbit:topic-exchange name="myExchange">
	    <rabbit:bindings>
	        <rabbit:binding queue="marketDataQueue"  pattern="app.stock.quotes.nasdaq.*"/>
	    </rabbit:bindings>
	</rabbit:topic-exchange>
      
	
	<bean id="stockController" class="com.gxkj.taobaoservice.controllers.demo.stocks.ui.StockController">
		<property name="stockServiceGateway" ref="stockClientServiceGateway"/>
	</bean>
	
	<bean id="clientHandler" class="com.gxkj.taobaoservice.controllers.demo.stocks.handler.ClientHandler">
		<property name="stockController" ref="stockController"/>
	</bean>	
	
	
	
</beans>